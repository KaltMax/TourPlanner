<UserControl x:Class="TourPlanner.UI.Views.EditTourLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TourPlanner.UI.Views"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">
    <Border BorderThickness="2" BorderBrush="Gray" CornerRadius="5" Padding="5" Margin="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Edit Tour Log Forms -->
                <StackPanel x:Name="EditTourLogForms" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch" Margin="10, 5">
                    <TextBlock HorizontalAlignment="Center" Foreground="LightGray" FontWeight="Bold" FontSize="20" Margin="10, 5" Text="Edit Tour Log"/>

                    <Separator Margin="0,0,0,20"/>

                    <StackPanel>
                        <Label Content="Total Distance (km)" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5" Foreground="LightGray" FontSize="14" FontWeight="Bold"/>
                        <TextBox VerticalAlignment="Center" HorizontalAlignment="Stretch" Padding="5" Margin="10, 5" FontWeight="Bold" Background="LightGray" TextWrapping="Wrap" Text="{Binding TotalDistance, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel>
                        <Label Content="Total Time (min)" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5" Foreground="LightGray" FontSize="14" FontWeight="Bold"/>
                        <TextBox VerticalAlignment="Center" HorizontalAlignment="Stretch" Padding="5" Margin="10, 5" FontWeight="Bold" Background="LightGray" TextWrapping="Wrap" Text="{Binding TotalTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel>
                        <Label Content="Comment" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5" Foreground="LightGray" FontSize="14" FontWeight="Bold"/>
                        <TextBox VerticalAlignment="Center" HorizontalAlignment="Stretch" Padding="5" Margin="10, 5" FontWeight="Bold" Background="LightGray" TextWrapping="Wrap" Text="{Binding Comment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Center">
                        <Label Content="Difficulty" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5" Foreground="LightGray" FontSize="14" FontWeight="Bold"/>
                        <ComboBox FontWeight="Bold" Padding="5" SelectedValue="{Binding Difficulty, Mode=TwoWay}" SelectedValuePath="Tag">
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="1" Content="1 - Very Easy"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="2" Content="2 - Easy"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="3" Content="3 - Medium"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="4" Content="4 - Hard"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="5" Content="5 - Very Hard"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Center">
                        <Label Content="Rating" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5" Foreground="LightGray" FontSize="14" FontWeight="Bold"/>
                        <ComboBox FontWeight="Bold" Padding="5" SelectedValue="{Binding Rating, Mode=TwoWay}" SelectedValuePath="Tag">
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="1" Content="1 - Very Bad"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="2" Content="2 - Bad"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="3" Content="3 - Medium"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="4" Content="4 - Good"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="5" Content="5 - Very Good"/>
                        </ComboBox>

                    </StackPanel>

                    <!-- Edit Tour Log Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5, 20">
                        <Button Style="{StaticResource RoundedButtonStyle}" Padding="10" Width="100" Height="40" Content="Save" Margin="5" Command="{Binding SaveCommand, Mode=OneWay}"/>
                        <Button Style="{StaticResource RoundedButtonStyle}" Padding="10" Width="100" Height="40" Content="Cancel" Margin="5" Command="{Binding CancelCommand, Mode=OneWay}"/>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>