<UserControl x:Class="TourPlanner.UI.Views.AddNewTourView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.UI.Views"
             mc:Ignorable="d" 
             Height="Auto" Width="Auto">

    <Border BorderThickness="2" BorderBrush="Gray" CornerRadius="5" Padding="5" Margin="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Tour input forms -->
                <StackPanel x:Name="AddNewTourForms" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch" Margin="10, 5">
                    <TextBlock HorizontalAlignment="Center" Foreground="LightGray" FontWeight="Bold" FontSize="20" Margin="10, 5" Text="Add New Tour"/>

                    <Separator Margin="0,0,0,20"/>

                    <StackPanel>
                        <Label Content="Tour Name" Foreground="LightGray" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5"/>
                        <TextBox VerticalAlignment="Center" Background="LightGray" FontWeight="Bold" HorizontalAlignment="Stretch" Padding="5" Margin="10, 5" TextWrapping="Wrap" Text="{Binding TourName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel>
                        <Label Content="Tour Description" Foreground="LightGray" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5"/>
                        <TextBox VerticalAlignment="Center" Background="LightGray" FontWeight="Bold" HorizontalAlignment="Stretch" Padding="5" Margin="10, 5" TextWrapping="Wrap" Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel>
                        <Label Content="From" Foreground="LightGray" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5"/>
                        <TextBox VerticalAlignment="Center" Background="LightGray" FontWeight="Bold" HorizontalAlignment="Stretch" Padding="5" Margin="10, 5" TextWrapping="Wrap" Text="{Binding From, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel>
                        <Label Content="To" Foreground="LightGray" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5"/>
                        <TextBox VerticalAlignment="Center" Background="LightGray" FontWeight="Bold" HorizontalAlignment="Stretch" Padding="5" Margin="10, 5" TextWrapping="Wrap" Text="{Binding To, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Center">
                        <Label Content="Transport Type" FontSize="14" Foreground="LightGray" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5"/>
                        <ComboBox FontWeight="Bold" Padding="5" SelectedValue="{Binding TransportType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag">
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="Car" Content="Car"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="Walking" Content="Walking"/>
                            <ComboBoxItem FontWeight="Bold" Padding="5" Tag="Bike" Content="Bike"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Create Custom Tour forms buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5, 20">
                        <Button Style="{StaticResource RoundedButtonStyle}" Padding="10" Width="100" Height="40" Content="Save" Margin="5" Command="{Binding SaveCommand}"/>
                        <Button Style="{StaticResource RoundedButtonStyle}" Padding="10" Width="100" Height="40" Content="Cancel" Margin="5" Command="{Binding CancelCommand}"/>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>
