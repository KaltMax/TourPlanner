<UserControl x:Class="TourPlanner.UI.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border BorderThickness="2" BorderBrush="Gray" CornerRadius="5" Padding="5" Margin="10">
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Content="Search" FontWeight="Bold" FontSize="20" Foreground="LightGray" VerticalAlignment="Center" Grid.Column="0"/>
            <TextBox x:Name="SearchTextBox" FontSize="16" Background="LightGray" Margin="5,0" Padding="5" VerticalAlignment="Center" Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Search" Style="{StaticResource RoundedButtonStyle}" Width="150" Margin="5,0" Grid.Column="2" Command="{Binding SearchCommand}"/>
            <Button Content="Reset" Style="{StaticResource RoundedButtonStyle}" Width="150" Margin="5,0" Grid.Column="3" Command="{Binding ResetSearchCommand}"/>
        </Grid>
    </Border>
</UserControl>