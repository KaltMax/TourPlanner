<UserControl x:Class="TourPlanner.UI.Views.MenuBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Menu Background="#121214" FontSize="14" Foreground="LightGray">
        <MenuItem Header="Files" Padding="5" FontSize="14">
            <MenuItem Header="Import Tours" FontSize="14" Foreground="Black" Command="{Binding ImportToursCommand}"/>
            <MenuItem Header="Export Tours" FontSize="14" Foreground="Black" Command="{Binding ExportToursCommand}"/>
            <Separator />
            <MenuItem Header="Export Tour Report" FontSize="14" Foreground="Black" Command="{Binding ExportTourReportCommand}"/>
            <MenuItem Header="Export Tour Summary" FontSize="14" Foreground="Black" Command="{Binding ExportTourSummaryCommand}"/>
            <Separator/>
            <MenuItem Header="Exit" FontSize="14" Foreground="Black" Command="{Binding ExitCommand}"/>
        </MenuItem>

        <MenuItem Header="Actions" Padding="5" FontSize="14" Foreground="LightGray">
            <MenuItem Header="Refresh Tours" FontSize="14" Foreground="Black" Command="{Binding ReloadToursFromDbCommand}"/>
            <Separator/>
            <MenuItem Header="Add Tour" FontSize="14" Foreground="Black" Command="{Binding AddTourCommand}"/>
            <MenuItem Header="Delete Tour" FontSize="14" Foreground="Black" Command="{Binding DeleteTourCommand}"/>
            <MenuItem Header="EditTour" FontSize="14" Foreground="Black" Command="{Binding EditTourCommand}"/>
            <Separator/>
            <MenuItem Header="Add Tour Log" FontSize="14" Foreground="Black" Command="{Binding AddTourLogCommand}"/>
            <MenuItem Header="Delete Tour Log" FontSize="14" Foreground="Black" Command="{Binding DeleteTourLogCommand}"/>
            <MenuItem Header="Edit Tour Log" FontSize="14" Foreground="Black" Command="{Binding EditTourLogCommand}"/>
        </MenuItem>

        <MenuItem Header="Help" Padding="5" FontSize="14" Foreground="LightGray">
            <MenuItem Header="View Help" FontSize="14" Foreground="Black" Command="{Binding ViewHelpCommand}"/>
        </MenuItem>
    </Menu>
</UserControl>